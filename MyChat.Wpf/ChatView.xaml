<UserControl x:Class="MyChat.Wpf.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <Border x:Name="HeaderBorder" Grid.Row="0" Background="#244179" Height="32">
            <TextBlock x:Name="HeaderText" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="White" Text="Chat (WPF)"/>
        </Border>

        <ListBox x:Name="MessageList" Grid.Row="1" />

        <Grid Grid.Row="2" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputText" Grid.Column="0" VerticalContentAlignment="Center"/>
            <Button x:Name="SendButton" Grid.Column="1" Margin="4,0,0,0" Content="Senden"/>
            <Button x:Name="ReloadButton" Grid.Column="2" Margin="4,0,0,0" Content="Reload"/>
        </Grid>
    </Grid>
</UserControl>

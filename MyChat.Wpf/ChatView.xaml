<UserControl x:Class="MyChat.Wpf.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F7FB">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="HeaderBorder" Grid.Row="0" Background="#244179" Height="32">
            <TextBlock x:Name="HeaderText" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="White" Text="Chat (WPF)" FontWeight="SemiBold"/>
        </Border>

        <ScrollViewer x:Name="MessageScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12">
            <StackPanel x:Name="MessagePanel" Orientation="Vertical"/>
        </ScrollViewer>

        <Border Grid.Row="2" Background="White" BorderBrush="#DCE2EA" BorderThickness="1,1,1,0" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <RichTextBox x:Name="InputText"
                                 VerticalContentAlignment="Center"
                                 Padding="10,6"
                                 BorderBrush="#C8D2E2"
                                 BorderThickness="1"
                                 Height="34"
                                 VerticalScrollBarVisibility="Hidden"/>
                    <TextBlock x:Name="InputPlaceholder"
                               Margin="14,8,0,0"
                               Foreground="#818C9E"
                               IsHitTestVisible="False"
                               Text="Nachricht ..."/>
                    <Popup x:Name="MentionPopup" Placement="Relative" PlacementTarget="InputText" StaysOpen="False" AllowsTransparency="True">
                        <Border BorderBrush="#C8D2E2" BorderThickness="1" Background="White" CornerRadius="4">
                            <ListBox x:Name="MentionList" Width="180" MaxHeight="120"/>
                        </Border>
                    </Popup>
                </Grid>

                <Button x:Name="SendButton" Grid.Column="1" Margin="6,0,0,0" Content="➤" FontWeight="Bold" Background="#244179" Foreground="White" BorderThickness="0"/>
                <Button x:Name="ReloadButton" Grid.Column="2" Margin="6,0,0,0" Content="Reload"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
